'use client';
import {useState,type FormEvent} from 'react';
import {Button} from '@/components/ui/button';
import {Input} from '@/components/ui/input';
import {Eye,EyeOff,LockKeyhole} from 'lucide-react';
export default function Login(){const[busy,setBusy]=useState(false),[error,setError]=useState(''),[show,setShow]=useState(false);
async function login(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setError('');const form=new FormData(event.currentTarget);try{const res=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(form))});const data=await res.json();if(!res.ok)throw Error(data.error||'Sign-in failed. Please try again.');window.location.replace('/admin');}catch(e){setError(e instanceof Error?e.message:'Sign-in failed. Please try again.');setBusy(false)}}
return <main className="admin-gate"><a className="admin-brand" href="/">learn<span>2x</span></a><div className="admin-gate-card"><div className="eyebrow"><LockKeyhole size={18}/> TUTOR WORKSPACE</div><h1>Welcome back.</h1><p>Sign in to view your students and demo requests.</p><form onSubmit={login}><label>Email<Input name="email" type="email" placeholder="Your admin email" autoComplete="username" required maxLength={254}/></label><label>Password<div className="admin-password-input"><Input name="password" type={show?'text':'password'} autoComplete="current-password" required minLength={1} maxLength={128}/><Button type="button" variant="ghost" size="icon" aria-label={show?'Hide password':'Show password'} onClick={()=>setShow(v=>!v)}>{show?<EyeOff size={18}/>:<Eye size={18}/>}</Button></div></label>{error&&<p className="error" role="alert">{error}</p>}<Button type="submit" className="submit-button" disabled={busy}>{busy?'Signing in…':'Sign in'}</Button></form><a className="admin-back" href="/">Back to the website</a></div></main>}
